using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class BotoesObjetosManager : MonoBehaviour
{

    BuildingManager buildingManager; //conecta o building manager a esse codigo
    [SerializeField] GameObject botaoObjetoPrefab;

    // Start is called before the first frame update
    void Start()
    {
        //for (int i = 0; i < buildingManager.objects.Length; i++)
        //{
        //    GameObject button = Instantiate(botaoObjetoPrefab);
        //    GetComponent<>(). = buildingManager.objects[i].dados.
        //}
    }
}
